<UserControl x:Class="Hdc.Controls.RegionIndicatorMarker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300"
             x:Name="Root">
    <Grid>
        <Rectangle Stroke="{Binding ElementName=Root, Path=Stroke}"
                   StrokeThickness="{Binding ElementName=Root, Path=StrokeThickness}"
                   StrokeDashArray="{Binding ElementName=Root, Path=StrokeDashArray }"></Rectangle>
    </Grid>
</UserControl>
